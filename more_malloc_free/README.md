# C - More malloc, free

## Description
This project continues the exploration of dynamic memory allocation in C, covering functions like `malloc_checked`, `string_nconcat`, `_calloc`, and `array_range`.

## Project Tasks
* **0. Trust no one**: Function that allocates memory using `malloc` and exits with status `98` if it fails.
